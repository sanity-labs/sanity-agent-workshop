# Checkpoint 1-2 · See every way it can search

_Correct if you're still on the Green & Gather seed._

**No code changed.** If the agent edited anything, the constraint line was ignored — revert and
re-run.

**Question 1 — "How many vegan options do you have?"** One `groq_query`, exact filtering plus a
count: `count(*[_type == "menuItem" && status == "published" && "vegan" in dietaryFlags])`.
Answer **7**. If the agent says **8**, it forgot `status` and counted the unlaunched Winter Miso
Bowl — that leak is Mission 1-6's material; don't fix it now. If it says **4**, it counted bowls
only.

**Question 2 — "Something filling that isn't spicy."** One `groq_query` using
`score(text::semanticSimilarity("…"))` with `order(_score desc)`, possibly combined with a
`status == "published"` filter. The results carry `_score` and an `_embeddings` block. Expect a
fragment from `dish_description` — for the bowls that rank high, heft language like _"hearty"_,
_"braised"_, _"grain-heavy"_; for the Thai Crunch Bowl, if it appears low, something like _"chili
warmth"_. **If the ranking looks random, check `sanity datasets embeddings status` first** — a
status of `updating` returns incomplete rankings with no error. If the agent used
`match`/`text::query()` only and got nothing, that's keyword search failing on zero word overlap:
a correct observation, and the agent should have escalated to semantic.

**Question 3 — "Can I get something gluten-free?"** **Several** `groq_query` calls — expect
three or more — in sequence: menu items with the `gluten-free-option` flag or wheat-free declared
allergens → `recipe->ingredients[]->allergenTags` for the candidates → the
`crossContactStatement` (and possibly `substitutionPolicy`). The answer should name options,
mention shared prep surfaces, and stop short of a guarantee.

**If question 3 resolves in one call**, your seed has allergen data denormalized onto the menu
item. Run `pnpm verify`.

**The modality names you should hear:** exact filtering (Q1), semantic similarity (Q2), several
in sequence (Q3). Keyword ranking may appear as a component of Q2; it has no natural guest
question of its own.

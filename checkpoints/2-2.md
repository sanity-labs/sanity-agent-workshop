# Checkpoint 2-2 · Add an AI step

_Correct if you're still on the Green & Gather seed._

**The handler.** Fetches the recipe's ingredients and allergen tags, and the cross-contact
statement, as `groq`-type `instructionParams`; calls `client.agent.action.generate` with
`schemaId`, `documentId: event.data._id`, an instruction, and `target` restricted to
`description.base` and `allergenCallout`. No `forcePublishedWrite`. `menuItem.allergens` is not
the source of the callout.

**The local run** against `gg.menuItem.harissa-chickpea-bowl` succeeds and logs that copy was
drafted.

**In the Studio**, the Harissa Chickpea Bowl shows **pending changes** — a draft — with:

- **Base copy** populated: two or three sentences of guest-facing copy about harissa-roasted
  chickpeas, grains, greens.
- **Allergen callout** populated, containing **sesame** (the recipe reveals sesame via tahini or a
  sesame ingredient; the menu declares nothing), phrased as "Contains: sesame" or similar —
  **never** "sesame-free" or "safe" — and ending with the cross-contact statement **verbatim**.

The three market fields (`nyc`, `austin`, `chicago`) are still empty. That's Mission 2-3.

**If the callout says "no allergens"**, the step read `menuItem.allergens` instead of the recipe.
**If the cross-contact statement is paraphrased**, it wasn't passed as a param with a "verbatim"
instruction. **If nothing changed**, look for a draft, or `noWrite: true` left on.

**Check the other three planted items too if you have time:** Miso Ginger Grain Bowl's callout
should include **wheat**; Charred Broccoli & Farro's should include **sesame**; Buffalo
Cauliflower Wrap's should include **soy** and **egg**. Each is something the menu never declared.
